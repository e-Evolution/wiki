{
  "name": "erpya-docs",
  "version": "0.1.0",
  "private": true,
  "description": "ERpya documentation site (docmd) — migrated from erpcya/docs VuePress 2",
  "scripts": {
    "dev": "docmd dev",
    "build": "docmd build",
    "validate": "node scripts/ci/ci-validate.mjs .",
    "check:images": "node scripts/ci/check-images.mjs docs assets"
  },
  "devDependencies": {
    "@docmd/core": "0.9.4"
  },
  "engines": {
    "node": ">=20"
  }
}
