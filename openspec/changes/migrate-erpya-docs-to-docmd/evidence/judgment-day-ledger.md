# Judgment Day — Migration Completeness (Round 1 ledger)

- **Date**: 2026-09-04
- **Target (immutable)**:
  - NEW site: https://e-evolution.github.io/wiki/ (docmd 0.9.4, subpath)
  - OLD site (reference, still live): https://docs.erpya.com/ (VuePress 2 + vuepress-theme-hope)
  - Repo: e-Evolution/wiki @ main `5472c9fd8d9ff7bc1a2a50b2b3c057e5925b59fa`
  - Old source: /tmp/erpcya-docs @ `eb33612` (2026-06-29, up to date)
  - Oracles: old sitemap 1,284 URLs (1,085 `.html` + 199 trailing-slash); new sitemap 2,051 URLs (all trailing-slash)
- **Judges**: jd-judge-a, jd-judge-b (blind, parallel, identical scope). Initial runs hit the 60-min runner timeout at ~20/21 turns; continued once each from their own `/tmp/judge{A,B}-*` scratch (blindness preserved: A never read B's files and vice versa). Final JSON results received from both.
- **Parent adjudication**: scope of old home/blog verified against /tmp/erpcya-docs (see F3/F4 notes).

## Confirmed (both judges) — SEVERE

| ID | Sev | Finding | Evidence |
|----|-----|---------|----------|
| **F1** | MAJOR | The entire old `.html` URL-form class is dead: **1,085 of 1,284** old-sitemap URLs (84.5%, every leaf page URL) return **404 as-is** on the new site; the build emits zero `.html` page files (only `site/404.html`). No redirects deployed (config map = 1 entry, for the %20 page). Content is NOT lost: every page serves 200 at its trailing-slash URL and no-slash 301→200. This is a material URL-continuity/SEO break for every old bookmark, indexed URL, and mirror (old sitemap host docs-md.erpya.com). | A: 13/13 sampled `.html` URLs old-200/new-404 (formmatrix.json); B: 11/11 sampled + `find site -name '*.html' ! -name index.html` → exactly 1 file (404.html) → deterministic for the whole class; parent: 20/20 random sample 404 + full census (1,283 direct-map + 1 %20 miss) |

## Suspect (single judge) — recorded, NOT auto-fixed

| ID | Sev (as reported) | Judge | Finding | Parent adjudication |
|----|-----|-------|---------|---------------------|
| **F2** | MINOR | B | %20 page edge: no-slash form `%20intercompany-process` → 301 with **raw unencoded space in the Location header** (invalid per RFC 3986; curl follow dies, browsers self-repair). Exactly 1 URL, not a class. as-sitemap `.html` form is part of F1; trailing-slash form = 200 meta-refresh stub; slugified = 200. | Verified deterministic (header bytes). Fixing F1 (adding the `.html` form) resolves the as-sitemap form; the no-slash directory-canonicalization 301 is a GitHub-Pages behavior over the literal-space redirect directory — documented as accepted edge (browsers repair; 1 URL). |
| **F3** | CRITICAL | A | New root page shows none of the old root's visible content: old home had 3 hand-written project cards (ADempiere 3.9.4, Nueva Interfaz, Discord), 9 latest-release cards (H1 "Fecha de Liberación: …"), 4 blog-aggregate links (/article/ 310, /category/ 7, /tag/ 481, /timeline/ 310), ~40 release download links, footer (377 links, 5 images). New root: bare page (73 nav links, 1 image, no H2). | **Adjudicated: presentation/theme gap, not content loss.** Old home = `src/README.md` with EMPTY body: all visible content was theme-hope-generated (frontmatter `projects:` + BlogHome layout aggregating `article: true` posts). All underlying content migrated: the 9 release cards = latest posts among the **712 migrated release pages** in `/downloads/updates/…`; cards/aggregate links = theme chrome. Additionally (parent): new `docs/index.md` still carries the dead VuePress frontmatter (`home: true`, `layout: BlogHome`, `projects:`, `bgImage:`, `footer:`) — harmless to docmd, should be cleaned when the home is adapted. Fix = editorial (hand-written home section + generated aggregate indexes) → **owner product decision**. |
| **F4** | MAJOR | A | Blog aggregation views absent: new /article/, /category/, /tag/, /timeline/ → 404 (old: 200). 0 article URLs in new sitemap. Structurally invisible to the 1,284-URL coverage check (old sitemap also had 0 /article/ entries) — the "every old URL is accounted for" claim in redirects-manifest.md does not cover these 4 theme-generated URLs. | **Adjudicated: theme-generated index views, content intact.** The 310 "articles" = the 712 release pages with `article: true` (all migrated as regular pages). The 4 aggregate URLs were generated by the blog plugin, not source files. Editorial restoration possible (static generated indexes) → **owner product decision** (note: /tag/ = 481 tags; full parity would be a large static surface — pragmatic variants: /article/ + /category/ + /timeline/ only, or accept as adapted). |
| **F5** | MAJOR | A | `redirects-manifest.md` (D5 cutover checklist) materially wrong: claims "Source URLs covered: 1284 (expected 1284)" and "Identity: 1283" — identity was computed after normalizing `.html`→trailing-slash, so 1,085 non-identity URLs are presented as identity; "a zero config-map means the cutover needs no redirects" is false for the `.html` class. | Derivative of F1 (same root cause). Corrected manifest regeneration belongs in the F1 fix work unit. |

## INFO (verified OK / non-actionable)

- **llms.txt**: 2,051 URL lines, 0 outside `https://e-evolution.github.io/wiki`; byte-identical live vs local build; matches build page count. (both judges)
- **Version trees**: 7/7 non-current roots + 14-21 sampled deep pages 200; all 8 version ids present in the switcher HTML; /rs-5.x/ 404 by design (current = /). (both)
- **Assets**: 40/40 sampled live image URLs 200 (incl. 23 from the 310-image business-partner page); check-images PASS (0 missing of 5,673 refs); ci-validate PASS (real broken 13 = pre-existing baseline, 0 new). (both)
- **Content fidelity** (sampled 8-10 pages/section): title/heading/body parity; new-site differences are additive chrome (h1 "ERpya Docs", TOC, ": ERpya Docs" title suffix). (both)
- **Build**: fresh build exit 0, 2,051 pages ~10-12s; index.html count 2,052 = pages + 1 %20 redirect stub; 3 non-blocking normaliser warnings. (both)
- **MCP**: no MCP *web manifest* in site/ — by design: docmd 0.9.4 web AI artifacts = llms.txt/llms.json/llms-full.txt + okf/; the MCP server is local stdio (6/6 tools verified WU1.5). A's "verify intent" note resolved: intent = local server, no web manifest required. (A, resolved by parent)
- **Coverage census**: 1,284 old = 1,283 direct-map + 1 (%20, redirect-handled); 767-768 extra new URLs = version trees + slugified page, all expected. (both)

## Verdict state

- Round 1 complete (both judges delivered). **Terminal verdict pending**: decision gate = confirmed severe finding (F1) → round-1 correction requires owner authorization; F3/F4 are owner product decisions (suspect, A-only).
- Next: owner decision on fix scope → bounded fix (jd-fix-agent) → scoped re-judgment over ledger + fix delta → terminal APPROVED | ESCALATED.

## Skill resolution

- `judgment-day` protocol: /Users/e-Evolution/.agents/skills/judgment-day/SKILL.md (+ references/prompts-and-formats.md) — parent
- Paths injected to both judges: /Users/e-Evolution/.agents/skills/cognitive-doc-design/SKILL.md
- Result: paths-injected

## Re-judgment (scoped: frozen ledger + fix delta bdd011b only)

- **Fix delta**: commit `bdd011b` (1,086 redirects with RELATIVE targets + oracle + manifest rewrite), deployed (run 33936968461). Fix round-1 had a v1→v2 correction: v1 emitted root-absolute targets (404 at subpath — parent-caught on the already-deployed %20 stub); v2 uses docmd's own relative-target convention (base-independent: subpath now, custom-domain root later, no regeneration at flip).
- **jd-judge-a**: zero findings. F1: resolved (10/10 .html 200 + all 10 meta-refresh targets resolve 200). F5: resolved. No regressions (pages 200; sitemap/llms 2,051, 0 .html indexed; local 2,052; ci-validate PASS 13/13/0). No fix-caused defects (config: only `redirects` changed; generator assertions verified).
- **jd-judge-b**: one MINOR (info-tier): manifest lines 40/78/102 still describe PRE-fix behavior — the fix made the site better than documented (see below). F1: resolved (11/11 sample + 3 E2E chains; oracle cross-check: 0 missing, 0 wrong, all 1,086 `to` resolve to built pages). F5: resolved. No regressions; no fix-caused functional defects.
- **Parent verification of the B-MINOR claims (all deterministic, 2026-09-05)**: (1) %20 no-slash now **200 direct** (meta-refresh stub via GitHub Pages .html-extension guessing) — the raw-space 301 edge is GONE, i.e. **F2 is resolved by the fix**, not merely accepted; (2) %20 trailing-slash = 200, 0 redirects (1 hop, not the documented 2); (3) leaf no-slash in general = **200 direct** via the same .html guessing (e.g. /community/code-of-conduct → stub `url=code-of-conduct/`); dir-index no-slash keeps its 301→slash as expected. Residual = documentation-only: 3 stale manifest lines (generator template) to align with post-fix measurements.

## Terminal verdict

```yaml
target_identity: repo main 5472c9f (original freeze) -> 056fef7 (ledger) -> bdd011b (fix); live https://e-evolution.github.io/wiki/ (deploy run 33936968461); old reference docs.erpya.com @ erpcya/docs eb33612
round: 1 fix round (v1+v2, one correction transaction) + 1 scoped re-judgment (budget: 2/2 fix rounds, 2/2 re-judgments)
confirmed: [F1 MAJOR .html class dead (1,085 URLs), F5 MAJOR manifest identity wrong]
suspect: [F2 MINOR %20 raw-space 301 (RESOLVED by fix - Pages .html guessing), F3 home cards (owner-deferred product decision), F4 blog aggregate views (owner-deferred product decision)]
contradictions: []
info: [manifest lines 40/78/102 stale pre-fix wording (B-MINOR, doc-only), llms 2,051 OK, 7 version trees OK, assets OK, build/gates OK, 13 baseline parity, MCP = local stdio by design]
fix_work_units: [WU-JD-1 .html-class redirects (1,086 relative-target entries, oracle committed), WU-JD-2 manifest D5 rewrite with measured semantics]
scoped_rejudgment: approved (A: zero findings; B: one MINOR doc-only)
terminal_state: approved
skill_resolution: paths-injected (judgment-day SKILL.md + prompts-and-formats.md parent; cognitive-doc-design.md both judges + fix actor)
```

**JUDGMENT: APPROVED ✅**

Open (non-blocking, owner-owned): F3 home cards + F4 blog aggregate views (explicitly deferred at the decision gate — theme-hope presentation, content fully migrated under /downloads/updates/); 3 stale manifest wording lines (generator template follow-up).
