# WU1.5 Pilot AI Verification — Report

- Date: 2026-09-03
- Executor: orchestrator (inline)
- Ledger: objective "WU1.5 pilot AI verification", token `sha256:1f9c803f…`
- Final Phase 1 unit.

## 1. llms endpoints at the site root (built site, final config)

All three artifacts generated by the build and served from the site root:

| artifact | size | content |
|---|---|---|
| `llms.txt` | 238 KB | page list |
| `llms.json` | 430 KB | `{title, description, pages[2051]}` |
| `llms-full.txt` | 5.4 MB | full-text context |

- **Absolute canonical URLs**: every entry uses `https://e-evolution.github.io/wiki/…`
  (the D8 `url` value, with the Phase 0–2 subpath) in both `llms.txt` and
  `llms.json` — spot-checked across modules and version trees.
- **Sampled pilot pages listed**: all 9 basic-rules pages and the 4
  master-data pages appear (e.g.
  `…/wiki/docs/master-data/business-partner/`, `…/wiki/docs/basic-rules/login-2fa/`).

## 2. Per-page `llms: false` opt-out — demonstrated and reverted

- Added `llms: false` to the frontmatter of one pilot page
  (`docs/docs/basic-rules/login-2fa.md`), rebuilt (exit 0):
  - `login-2fa` **absent** from `llms.txt` (0 occurrences) and from `llms.json`
    (2,051 → 2,050 pages).
  - Sibling `login-keycloak` still listed — the opt-out is per-page, not
    per-directory.
- Reverted (the file is back to its WU0.5 state; a content page is not
  excluded from AI context by default — exclusion is an owner editorial
  decision). Final rebuild restored 2,051 pages with `login-2fa` present.

## 3. MCP smoke — all 6 tools reachable (stdio JSON-RPC, 0.9.4)

`npx -y @docmd/core@0.9.4 mcp` over stdio (same handshake as
`scripts/ci/ci-validate.mjs`; non-JSON log lines skipped as non-frames):

- `initialize` → `docmd-mcp-server` 0.9.4, protocol `2025-03-26`.
- `tools/list` → exactly the 6 expected tools:
  **`search_docs`, `list_docs`, `read_doc`, `get_config`, `validate_docs`,
  `get_llms_context`**.
- Live call `get_config` → OK (returns the config, incl.
  `url: https://e-evolution.github.io/wiki` and the dotted `versions` block).
- Live call `search_docs` with the pilot-only term **`authenticator`** → OK;
  hits `docs/docs/basic-rules/login-2fa.md` (line 11).

## Verdict

**PASS** — all three WU1.5 acceptance bullets verified. **Phase 1 complete**
(WU1.1–WU1.5, PR #10–#14): measurements recorded (OQ1 ADOPT dotted ids,
OQ2 GO), pilot slice verified with the switcher defect fixed, llms + MCP live.
