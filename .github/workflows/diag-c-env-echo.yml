name: diag-c-env-echo
on:
  push:
    branches: [main]
jobs:
  envtest:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
    permissions:
      contents: read
      pages: write
      id-token: write
    steps:
      - run: echo "env ok"
